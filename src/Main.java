public class Main {
    public static void main(String[] args) {
        // version 1.0
        int lado = 3, area;
        area = (int) Math.pow(lado, 2);
        System.out.println("el area=" + area);
    }
}